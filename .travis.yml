sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t angel/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true angel/react-test npm test

after_success:
  - docker build -t angel/multi-client ./client
  - docker build -t angel/multi-nginx ./nginx
  - docker build -t angel/multi-server ./server
  - docker build -t angel/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push angel/multi-client
  - docker push angel/multi-nginx
  - docker push angel/multi-server
  - docker push angel/multi-worker
